# Fetched k3s admin kubeconfig (server-rewritten to the container IP). Contains
# the cluster CA + admin client cert/key — never commit it. Regenerated by the
# fetch_kubeconfig local-exec (or the fetch_kubeconfig_command output) on apply.
kubeconfig
